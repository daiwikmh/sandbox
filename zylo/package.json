{
  "name": "zylo-exchange",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "compact compile contracts/exchange.compact managed/exchange",
    "test": "vitest run",
    "test:watch": "vitest",
    "enclave": "tsx enclave/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.19.0",
    "@types/node": "^22.20.2"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
